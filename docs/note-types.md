# Note Types

Note types and their fields will be documented here after connecting to Anki via AnkiConnect.

To retrieve from Anki once connected:
- `modelNames` — list all note types
- `modelFieldNames` — list fields for a given note type
